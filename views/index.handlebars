{{!-- <h2>Select your burger and eat up.</h2> --}}

<div class="container">
  <div class="row justify-content-md-center">
    <div id="toBeEaten" class="col col-md-4">
      <h4>Your Order</h4>
      <br>
      <ul>
        {{#each burgers}}
        {{#unless this.consumed}}
        <li>
          <p class="label"> {{this.burger_name}}
          <button data-id="{{this.id}}" class="delquote">Devour it!</button></p>
        </li>
        {{/unless}}
        {{/each}}
      </ul>
    </div>
    <div class="col-md-1">
      {{!-- Variable width content --}}
    </div>
    <div id="eaten" class="col col-md-4">
      <h4>In Your Stomach</h4>
      <br>
      <ul>
        {{#each burgers}}
        {{#if this.consumed}}
        <li>
          <p><span class="label">Burger:</span> {{this.burger_name}}</p>
        </li>
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>






  {{!-- <form class="create-form">

    <div class="form-group">
      <label for="burger">Burger:</label>
      <input type="text" id="burgerName" name="burger" placeholder="Enter burger name">
    </div>

    <button type="submit">Add the burger</button>
    <h4>Enter a burger you would like to eat and click "Submit".</h4>
  </form> --}}